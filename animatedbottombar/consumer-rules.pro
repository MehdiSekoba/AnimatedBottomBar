# Consumer ProGuard rules for the :animatedbottombar library.
#
# The library is pure Compose/Kotlin with no reflection, service loaders, or
# serialization, so no keep rules are required. Compose's own consumer rules
# (shipped with the Compose artifacts) handle everything the runtime needs.
